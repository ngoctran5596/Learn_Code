import React from 'react'
import { ScrollView, StyleSheet, Text, View } from 'react-native'
import { HeaderNav } from '@components'

export const Note = () => {
    return (
        <ScrollView>
            <HeaderNav title='Note'/>
        </ScrollView>
    )
}


const styles = StyleSheet.create({})
